# SSH Keys Auto-load Configuration
# List one SSH private key path per line
# Lines starting with # are ignored
# Use ~ for home directory (will be expanded)

# Common standard SSH keys (uncomment the ones you have):
# ~/.ssh/id_ed25519
# ~/.ssh/id_rsa
# ~/.ssh/id_ecdsa
# ~/.ssh/id_dsa

# Custom keys:
# ~/.ssh/work_key
# ~/.ssh/personal_key
